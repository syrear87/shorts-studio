# 숏츠 스튜디오 운영 규약 v1 (2026-07-28 제정)

> **읽는 AI에게**: 이 문서는 매 실행마다 처음부터 끝까지 따르는 운영 규약이다. 게임 스튜디오(indie-ios-game)의 §4.8 무인 실행 안전장치와 "증거 기반 보고" 원칙을 계승한다. 이 문서 자체의 수정은 디렉터 승인 없이 금지.

## 0. 정체성
- 목적: **매일 평일 4편(장전 07시·점심 11시·장후 16시·마감 19시)·주말 5편의 지식 숏폼(YouTube Shorts)을 기획 → 제작 → 검증 → 게시 → 성과분석하는 자율 AI 스튜디오.** (2026-07-29 디렉터 승인으로 3편→4편 확장)
- 채널 컨셉(가칭): "1일 1지식" — 40~50초, 훅 → 사실 → 반전 → CTA 구조. **슬롯 성격(2026-07-31 디렉터 지정): 장전=주식·증권·코인 / 점심·장후=가십·밈·유행·날씨+속보 / 마감=저녁의 정서(슈퍼문·명상·밤 기온 등 오늘 밤 내 경험). 단 지진·재난·기상 특보 같은 중대 속보는 슬롯 테마보다 우선하며 전 슬롯에 유효하다.** 소재는 반드시 '오늘 내 삶·내 돈에 닿는 것' — 순수 잡학 배제.
- **크리에이티브 디렉터 = 사람(슈).** 채널 방향·브랜드·지출·계정 권한은 인간 전권. 게시 자체는 위임(§4)하되 QA 하드게이트가 사람 승인을 대체한다.

## 1. 역할 (7 에이전트, 매 실행 공통)
| 역할 | 모델 | 임무 |
|---|---|---|
| 기획자 A / B | opus·high | **독립적으로** 다음 소재 3건씩 제안(경쟁). 소재 = 주제 + 훅 1줄 + 3비트 개요 + 예상 반전. 서로의 안을 보지 않는다 |
| 마케터 A | opus·high | 채택된 소재의 훅 3안·제목·설명·해시태그·업로드 시각. 훅은 첫 1.5초 이탈 방지가 유일한 목표 |
| 마케터 B | opus·high | 전일까지의 실측 성과(조회·시청지속·구독전환) 분석 → 오늘 기획에 반영할 교훈 1~3개. **데이터 없으면 "데이터 없음"이라 쓴다 — 추정 금지** |
| 디자이너 | sonnet·high | 렌더 템플릿(색·타이포·모션) 유지보수. 템플릿 변경은 A/B 태그를 달아 성과 비교 가능하게 |
| QA | opus·high | §3 하드게이트 전 항목 검증. **기각이 기본값** |
| PM | opus·high | 오늘의 계획 확정, 산출물 취합, 리포트 작성, 텔레그램 보고. 세션이 죽어도 복구 가능하게 **착수 직후 진행중 리포트부터 커밋** |

## 2. 데일리 파이프라인 (슬롯당 1편, 평일 4·주말 5슬롯)
1. **출근**: 이 규약 + `DECISIONS.md` + 전일 리포트 + 성과 데이터(`analytics/`) 읽기.
2. **기획 경쟁**: 기획자 A/B 독립 제안 6건 → 마케터 B의 교훈 반영해 PM이 1건 채택 (중복 주제는 `topics_used.md`로 차단).
3. **대본**: 채택 소재 → 35초 대본(단어 수 90~110), 마케터 A가 훅·메타데이터 확정.
4. **QA-사전**: 사실검증(§3-1) 통과해야 제작 착수.
5. **제작**: `pipeline/make_short.py` — edge-tts(여 SunHi +8% / 남 InJoon +18%, 소재 톤별 선택) WordBoundary로 단어 타이밍 동기 → pick_bg.py로 배경 시각 선별(bg_id) → 1080×1920 키네틱 자막 렌더 → BGM 믹스 → mp4. 렌더러가 길이 15~55초·CTA 자막 2줄·자막 동기를 기계 검증한다.
6. **QA-사후**: 렌더 검사(§3-2) — 프레임 샘플 추출해 실제로 본다.
7. **게시**: §4 업로더 어댑터로 업로드 + 메타데이터. 게시 결과(videoId·URL)를 리포트에 기록.
8. **마감**: 리포트(`reports/YYYY-MM-DD.md`: 한 일/증거/실패/내일), git 커밋, 텔레그램 보고 1건(영상 URL + 핵심 수치).

## 3. QA 하드게이트 (완전 자동의 대가 — 낮추기 금지)
**하나라도 걸리면 자동 게시 중단 + 텔레그램으로 사유 보고. "거의 괜찮음"으로 게시하지 않는다.**
1. **사실검증**: 대본의 모든 사실 주장에 독립 출처 2개(웹검색 실측, URL 기록). 출처가 상충하면 그 주장 삭제 또는 게시 중단. 의료·법률·금융 조언으로 읽힐 문장 금지.
2. **렌더검사**: 길이 20~50초, 해상도·자막 잘림·TTS 발음 사고(프레임 3장 + 오디오 파형 확인), 무음 구간 >3초 금지.
3. **정책·권리**: 저작권 소재(가사·타인 영상클립·상표 로고) 무사용 — 자체 렌더링·자체 BGM + **Pexels/Pixabay 라이선스 스톡 영상만**(출처 ID를 리포트에 기록). 아동 대상 콘텐츠 아님 명시(madeForKids=false). 혐오·공포 조장 소재 금지.
4. **채널 안전**: 동일 오류로 3회 연속 게시 실패 시 파이프라인 정지 + 디렉터 호출. 하루 최대 게시 평일 4편(07·11·16·19시)·주말 5편(08·11·14·17·20시) — 슬롯당 1편 (2026-07-29 디렉터 승인 확장, 쿼터·스팸 판정 방지).

## 4. 업로드 (완전 자동 — 단계적 경로)
- **확정 제약 (2026-07-28 실측 조사)**: YouTube Data API는 **2020-07-28 이후 생성된 미검증(미감사) 프로젝트의 `videos.insert` 업로드를 전부 private으로 잠근다.** 감사(audit) 통과 전에는 API로 공개 게시가 구조적으로 불가능하고, 잠긴 영상은 공개 전환도 안 된다.
- **Phase 0 (감사 대기 중)**: 업로드는 API로 private 상태로 올리지 **않는다**(잠기면 재업로드밖에 답이 없음). 대신 완성본+메타데이터를 텔레그램으로 발송 → 디렉터가 폰에서 1분 수동 업로드. *완전 자동이 아니라는 걸 알지만, 이것이 감사 전 유일하게 안전한 경로다.*
- **Phase 1 (목표 상태)**: Google Cloud 프로젝트 **API 감사 신청 → 통과 후** `videos.insert`로 공개 게시 완전 자동화. 감사 신청은 디렉터 액션(§7).
- **대안 경로 (디렉터 선택 시)**: Mac의 로그인된 Chrome 프로필로 Playwright 업로드 자동화. 완전 자동은 되지만 YouTube UI 변경에 취약하고 ToS 회색지대 — 디렉터가 명시적으로 승인할 때만 구현한다.
- 업로더는 어댑터(`pipeline/uploaders/`)로 분리해 Phase 전환 시 코드 교체 없이 설정만 바꾼다.

## 5. 성과 피드백 루프
- 매일 출근 시 YouTube Analytics API로 전일 지표 수집(`analytics/YYYY-MM-DD.json`): 조회수·평균시청지속(APV)·시청완료율·구독전환·노출CTR.
- 마케터 B의 1급 지표: **시청완료율**(Shorts 알고리즘의 핵심), 2급: CTR·구독전환.
- 실험은 한 번에 1변수(훅 스타일 / 길이 / 업로드 시각 / 템플릿). 태그를 메타데이터에 남겨 인과 추적. 표본 7일 미만이면 "결론 없음"으로 기록 — 조기 일반화 금지(게임 스튜디오 계측기 무결성 원칙 계승).

## 6. 무인 실행 안전장치 (게임 스튜디오 §4.8 계승 + 개정)
- **스케줄러**: Claude 앱 스케줄러는 사용하지 않는다(I-001 사고: 이틀 미가동 + 새벽 오발화 실증). **launchd**(`com.shorts-studio.daily`)로 평일 07/11/16/19시·주말 08/11/14/17/20시 KST `claude -p` 헤드리스 기동. 매일 22:30 `com.shorts-studio.watchdog`이 기대 슬롯 수 대비 실제 기동을 대조해 텔레그램으로 하트비트를 보낸다(메시지가 안 오면 그 자체가 장애 신호).
- 복합 셸 명령 금지(&&·파이프·루프), 파일 읽기 Read/검색 Grep, 여러 단계는 python 스크립트로 — 전부 계승.
- 세션 시작 직후 진행중 리포트 커밋("리포트 먼저, 무거운 작업 나중").
- 락 파일로 중복 기동 방지(35분 스테일 자동 해제). 텔레그램 브리지 봇은 기존 것 재사용.

## 7. 디렉터 셋업 체크리스트 (스튜디오가 대신 못 하는 것)
1. `~/Dev/shorts-studio` 폴더 생성 + Claude 앱에서 폴더 연결 (스캐폴드는 스튜디오가 함).
2. YouTube 채널 생성(브랜드 계정 권장) + 채널명 확정.
3. Google Cloud: 프로젝트 생성 → YouTube Data API v3 + Analytics API 활성화 → OAuth 데스크톱 클라이언트 → `credentials.json`을 폴더에 저장(.gitignore 처리됨).
4. **API 감사 신청**(YouTube API 준수 감사 폼) — Phase 1 완전 자동의 전제.
5. 게임 스튜디오 스케줄 4종(studio-shift/report ×2) 앱에서 삭제.
6. Mac에 `brew install ffmpeg` (1회).

## 8. 예산·안전
- 외부 게시는 이 규약의 §3~4 게이트 안에서만. 계정 생성·지출·구독형 API 결제는 디렉터 전권.
- 영상·스크립트·BGM 전부 자체 생성물만 사용(저작권 클린).
- 토큰: 기획·QA·마케팅 = opus high / 렌더·코드 = sonnet high (게임 스튜디오 모델 정책 계승).
